// Added unprotected routes to the list of routes that do not require authentication.

export const authRoutes = ['/auth'];

// export const authRoutesUpdated = authRoutes.flatMap((i) => [
//   `/en${i}`,
//   `/bn${i}`,
// ]);
